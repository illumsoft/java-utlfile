Place Oracle Instant Client into this directory
(maven expects to find ojdbc6.jar here).

Alternatively you can edit pom.xml to point to $ORACLE_HOME/jdbc/lib/ojdbc6.jar
which is installed with database or client.
